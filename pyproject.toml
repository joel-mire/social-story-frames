[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ssf"
version = "0.1.0"
description = "SSF project"
requires-python = ">=3.8"
dependencies = [
    "pyyaml",
    "pydantic>=2.0",
]

[tool.setuptools.packages.find]
where = ["src"]
